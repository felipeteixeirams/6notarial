import React from 'react';
import TopBar from '../TopBar/TopBar';
import Menu from '../Menu/Menu';

const App = () => {
  return (
    <React.StrictMode>
      <TopBar />
      <Menu />
    </React.StrictMode>
  );
}

export default App;
